#!/usr/bin/env node
import('../dist/src/cli.js')
  .then((m) => m.main())
  .then((code) => process.exit(code))
  .catch((err) => {
    process.stderr.write('mdc: fatal: ' + (err?.stack ?? err) + '\n');
    process.exit(1);
  });
